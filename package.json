{
  "name": "rickly-url-shortener",
  "type": "module",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "api": "bun --filter api dev",
    "client": "bun --filter client dev",
    "build": "bun --filter client build",
    "deploy": "bun run build && bun --filter api deploy:prod"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
